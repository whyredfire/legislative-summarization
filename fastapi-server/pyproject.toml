[project]
name = "fastapi-server"
version = "0.1.0"
description = "FastAPI app for running summarization model."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "nltk>=3.9.3",
    "transformers>=5.3.0",
    "uvicorn>=0.42.0",
]
