[project]
name = "fastapi-server"
version = "0.1.0"
description = "FastAPI app for running summarization model."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "nltk>=3.10.3",
    "transformers>=5.15.0",
    "uvicorn>=0.52.4",
]
