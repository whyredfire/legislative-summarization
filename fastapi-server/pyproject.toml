[project]
name = "fastapi-server"
version = "0.1.0"
description = "FastAPI app for running summarization model."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.138.1",
    "nltk>=3.9.4",
    "transformers>=5.12.1",
    "uvicorn>=0.49.0",
]
