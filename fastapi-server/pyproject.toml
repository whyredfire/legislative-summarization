[project]
name = "fastapi-server"
version = "0.1.0"
description = "FastAPI app for running summarization model."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.1",
    "nltk>=3.9.4",
    "transformers>=5.8.1",
    "uvicorn>=0.47.0",
]
