apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fastapi-model-cache
  labels:
    app: fastapi-server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
