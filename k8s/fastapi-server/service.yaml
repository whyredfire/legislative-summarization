apiVersion: v1
kind: Service
metadata:
  name: fastapi-server
  labels:
    app: fastapi-server
spec:
  type: ClusterIP
  selector:
    app: fastapi-server
  ports:
    - port: 8000
      targetPort: 8000
